{
    "type": "project",

    "repositories": [{
        "type": "path",
        "url": "../framework",
        "options": {
            "symlink": true
        }
    }],

    "require": {
        "zubzet/framework": "*@dev"
    },

    "config": {
        "sort-packages": true
    }
}
