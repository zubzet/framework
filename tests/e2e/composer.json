{
    "type": "project",

    "repositories": [{
        "type": "path",
        "url": "../framework",
        "options": {
            "symlink": true
        }
    }],

    "minimum-stability": "dev",
    "prefer-stable": true,

    "require": {
        "zubzet/framework": "*@dev"
    },

    "config": {
        "sort-packages": true
    }
}
