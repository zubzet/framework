{{-- App-owned anonymous component named "head". It shares the bare name of the
     framework's namespaced <x-zubzet::head/> on purpose: the two must stay
     isolated (neither shadows the other). Exercised by core/component-namespace. --}}
<div data-test="app-head">APP_HEAD_COMPONENT</div>
