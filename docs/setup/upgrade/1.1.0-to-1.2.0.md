# Migrating from v1.1.0 to v1.2.0

## Table of Contents

* [New Features](#new-features)
* [Breaking Changes](#breaking-changes)
* [Upgrade Steps](#upgrade-steps)


## New Features

### Logging

ZubZet now includes a built-in [**logging system**](../../../core-features/logging) powered by Monolog.
It supports two backends — **database** and **stream** — and is enabled by default.
The framework automatically records system events such as logins, page views, and REST errors.


## Breaking Changes

### Removed: Log category methods on models

The following methods have been removed from the base model class:

* `getLogCategoryIdByName()`
* `logAction()`
* `logActionByCategory()`

If your application calls any of these methods, replace them with the new `logger()` helper. See the [Logging documentation](../../../core-features/logging) for usage.

### Removed: `z_interaction_log_category` table

The `z_interaction_log_category` table has been dropped entirely. The `categoryId` column has also been removed from `z_interaction_log`.

### Removed: Log viewer in Admin Panel

The log viewer previously available in the Admin Panel has been removed.


## Upgrade Steps

---

### 1. Remove calls to deprecated model methods

Search your application code for calls to the removed methods and replace them with `logger()`:

```php
// Before
$this->logAction("Something happened");
$this->logActionByCategory("Something happened", "category");

// After
logger()->info("Something happened");
```

---

### 2. Run the database migration

The following schema changes are applied automatically when the framework updates.
If you manage migrations manually, apply them yourself:

```sql
DROP TABLE IF EXISTS `z_interaction_log_category`;
ALTER TABLE `z_interaction_log` DROP COLUMN IF EXISTS `categoryId`;
```

---

### 3. Configure logging (optional)

Logging is **enabled by default** with the database backend. To adjust the behavior, add the relevant keys to `z_config/z_settings.ini`:

```ini
logger_enabled = true
logger_type = database
logger_level = info
```

To disable logging entirely:

```ini
logger_enabled = false
```

See the [Logging documentation](../../../core-features/logging) for all available options.
